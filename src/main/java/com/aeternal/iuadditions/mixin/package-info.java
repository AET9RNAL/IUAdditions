/**
 * Mixin classes for this mod.
 *
 * Add @Mixin classes here and list them in boilerplate.mixins.json
 * under "mixins" (common), "client" (client-only), or "server" (server-only).
 *
 * Example:
 * <pre>
 * {@literal @}Mixin(SomeMinecraftClass.class)
 * public class SomeMinecraftClassMixin {
 *     {@literal @}Inject(method = "someMethod", at = @At("HEAD"))
 *     private void onSomeMethod(CallbackInfo ci) {
 *         // Your injection code
 *     }
 * }
 * </pre>
 */
package com.aeternal.iuadditions.mixin;
